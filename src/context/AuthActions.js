export const LoginStart = (user) => ({
  type:"LOGIN-START",
});

export const LoginSuccess = (user) => ({
  type:"LOGIN-SUCCESS",
});

export const LoginFailure = (user) => ({
  type:"LOGIN-FAILURE",
});